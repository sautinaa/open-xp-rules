Description: 'The user cleared the log one of the System, Install, Application logs. When the save and clear log option is selected, the log is saved in object.value'
EventDescriptions:
    - LocalizationId: 'corrname_Clearing_eventlog'
      EventDescription: 'User {subject.account.domain}\{subject.account.name} clean {object.name}'
